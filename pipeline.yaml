apiVersion: tekton.dev/v1beta1
kind: Pipeline
metadata:
  name: tax-calculator-pipeline
spec:
  tasks:
    - name: npminstall
      taskRef:
        name: npminstall

    - name: tests
      runAfter:
        - npminstall
      taskRef:
        name: tests

    - name: build
      runAfter:
        - tests
      taskRef:
        name: build